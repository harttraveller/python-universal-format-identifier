
- if loc
    - check ext
    - if success, return
    - else
        - if val, load
        - resolve bin
- if uri
    - check ext
    - if success, return
    - else
        - if val, fetch
        - resolve bin
- if raw (text)
- if bin
